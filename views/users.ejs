<!--users.ejs-->
<!DOCTYPE html>
<html lang="ja">

    <head>
        <meta charset="UTF-8">
        <meta name="viewpoint" content="width-device-width, initial-scale=1.0">
        <title>users</title>
    </head>

<body>
    <ul>
        <% for (const user of users) { %>
            <li><%= user.name %></li>
        <% } %>
    </ul>
</body>

</html>